# smithery.yaml
runtime: "container"
build:
  dockerfile: "Dockerfile"  # Path to your Dockerfile
  dockerBuildPath: "."       # Docker build context (current directory)
startCommand:
  type: "http"              # Specifies your server uses HTTP transport
